name: CI

on:
  push:
    branches:
      - 'main'
  pull_request: 
    branches:
      - 'main'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Neovim nightly
        uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: nightly

      - name: testing it out
        run: |
          set -x
          sudo apt update
          sudo apt install curl -y
          curl -s -L -o /tmp/nvim-linux64.tar.gz https://github.com/neovim/neovim/releases/download/nightly/nvim-linux64.tar.gz
          tar -xvf /tmp/nvim-linux64.tar.gz --directory /opt
          export PATH=$PATH:/opt/nvim-linux64/bin

          mkdir -p ~/work/.local/share/nvim/render/output
          sudo chmod 777 ~/work/.local/share/nvim/render/output
          abs_path="$(realpath ~/work/.local/share/nvim/render/output)"
          nvim -V15vlog.log +mode +'autocmd! CursorHold * :call v:lua.vim.api.nvim__screenshot("/home/runner/work/.local/share/nvim/render/output/something.cat") | sleep 5 | :qall!'
          cat vlog.log
          cd ~/work/.local/share/nvim/render/output
          ls -la
          cat something.cat || true
          nvim -V15vlog.log +mode +'autocmd! CursorHold * :call v:lua.vim.api.nvim__screenshot("/home/runner/work/.local/share/nvim/render/something.cat") | sleep 5 | :qall!'
          cat vlog.log
          cd ~/work/.local/share/nvim/render
          ls -la
          cat something.cat || true
          nvim -V15vlog.log +mode +'autocmd! CursorHold * :call v:lua.vim.api.nvim__screenshot("/home/runner/work/.local/share/nvim/something.cat") | sleep 5 | :qall!'
          cat vlog.log
          cd ~/work/.local/share/nvim/
          ls -la
          cat something.cat || true
          nvim -V15vlog.log +mode +'autocmd! CursorHold * :call v:lua.vim.api.nvim__screenshot("/home/runner/work/.local/share/something.cat") | sleep 5 | :qall!'
          cat vlog.log
          cd ~/work/.local/share/
          ls -la
          cat something.cat || true
          nvim -V15vlog.log +mode +'autocmd! CursorHold * :call v:lua.vim.api.nvim__screenshot("/home/runner/work/.local/something.cat") | sleep 5 | :qall!'
          cat vlog.log
          cd ~/work/.local/
          ls -la
          cat something.cat || true
          nvim -V15vlog.log +mode +'autocmd! CursorHold * :call v:lua.vim.api.nvim__screenshot("/home/runner/something.cat") | sleep 5 | :qall!'
          cat vlog.log
          cd ~
          ls -la
          cat something.cat || true
          set +x 


